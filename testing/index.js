const bicycle = {
    color: "blue",
    electric: false,
    start() {
        console.log("You begin to pedal the bike. ");
    }
};
bicycle.color = "green";
bicycle.start = function (name) {
    console.log("you can start the engine on your bicycle");
    return "It's working";
}

console.log(bicycle);
console.log(bicycle.color);

function hello(){
    return {studentName: name};

}

const hello = name => ({studentName:name});
const hello = name => ({studentName:name});
const hello = name => ({studentName:name});
const hello = name => ({studentName:name});
const hello = name => ({studentName:name});
const hello = name => ({studentName:name});
const hello = name => ({studentName:name});
